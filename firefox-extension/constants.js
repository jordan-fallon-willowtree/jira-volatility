const APPLE = 'apple'
const ANDROID = 'android'
const WEB = 'web'
const TE = 'te'

const MILLISECONDS_PER_WEEK = 1000 * 60 * 60 * 24 * 7

module.exports = { APPLE, ANDROID, WEB, TE, MILLISECONDS_PER_WEEK }
